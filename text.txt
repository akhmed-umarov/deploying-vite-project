npm create vite@latest 
потом выбираем имя проекта и после выбираем нужный фраймворк
SWC ускорение работы JEST тестов

.env => переменные окружения и получить к ним доступ можно так 
как на примере снизу

.env.local => те же самые переменные окружения только 
они попадают под гитИгнор но доступ так же с ним получает
без .local

console.log(import.meta.env.VITE__SOME__TEXT);
